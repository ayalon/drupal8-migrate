type: module
name: Migrate Menu
description: 'Imports menu structure from a csv file'
package: Migration
core: 8.x
dependencies:
  - migrate
  - migrate_plus
  - migrate_source_json
  - migrate_posts
config_devel:
  install:
    - migrate_plus.migration.post
    - migrate_plus.migration.comment
